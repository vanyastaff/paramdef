[build]
# Uncomment for faster builds with debug info
# rustflags = ["-C", "debuginfo=1"]

# LLD linker settings moved to .cargo/config.local.toml
# Copy config.local.toml.example to config.local.toml for faster local builds

[alias]
xtask = "run --package xtask --"
